# Quiz Events App (Django)

A technical interview task for **Formics Tech LLC**. This application allows users to take dynamic quizzes and view upcoming events.

## 🚀 Features
* **Quizzes:** Multiple choice questions with scoring logic.
* **Events:** List view of upcoming events.
* **Tech Stack:** Django (Backend), SQLite, Html,Tailwind CSS (Frontend).

## ⚙️ How to Run
1.  **Clone the repo:**
    ```bash
    git clone [https://github.com/SUJALrami/django-quiz-app.git](https://github.com/SUJALrami/django-quiz-app.git)
    cd django-quiz-app
    ```
2.  **Install requirements:**
    ```bash
    pip install -r requirements.txt
    ```
3.  **Run Migrations:**
    ```bash
    python manage.py migrate
    ```
4.  **Create Superuser (to add quizzes):**
    ```bash
    python manage.py createsuperuser
    ```
5.  **Run Server:**
    ```bash
    python manage.py runserver
    ```

## 👤 Author
**Sujal Rami**